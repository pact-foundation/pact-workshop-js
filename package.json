{
  "name": "pact-workshop-js",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm i --prefix consumer",
    "start": "npm start --prefix consumer",
    "lint": "cd consumer/ && npx eslint ."
  },
  "dependencies": {
    "concurrently": "8.2.1"
  }
}
