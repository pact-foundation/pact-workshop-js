{
  "name": "pact-workshop-js",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix consumer",
    "start": "npm start --prefix consumer"
  },
  "dependencies": {
    "@pact-foundation/pact": "^9.11.1",
    "concurrently": "^5.0.2"
  }
}
